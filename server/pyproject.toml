[project]
name = "jewelry-api"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy>=2.0",
  "psycopg2-binary>=2.9",
  "pydantic>=2",
  "python-multipart",
  "passlib[bcrypt]",
  "pyjwt",
]

[tool.uvicorn]
reload = true
